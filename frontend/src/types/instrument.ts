export type InstrumentType = {
    id: string;
    nome: string;
    marca: string;
    preco: number;
    ano_lancamento: number;
    imagem: string;
    categoria: string;
    qtd_estoque: number;
}